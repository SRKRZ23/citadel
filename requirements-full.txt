# CITADEL — full development requirements (with direct-inference engines).
#
# These deps are needed only if you want to run inference inside the Python
# process. For Streamlit Cloud deployment or talking to a separate Ollama /
# vLLM daemon over HTTP, use the slim requirements.txt instead.

# Base (same as requirements.txt)
streamlit>=1.33.0
matplotlib>=3.7.0
pandas>=2.0.0
numpy>=1.24.0
httpx>=0.27.0
cryptography>=41.0.0
PyNaCl>=1.5.0
PyYAML>=6.0

# Direct-inference engines (only needed for in-process GPU inference)
vllm>=0.4.0
transformers>=4.40.0
torch>=2.2.0
