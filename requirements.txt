# CITADEL — Streamlit-Cloud-compatible runtime dependencies.
#
# The full development environment (vLLM, torch, transformers) is documented
# in requirements-full.txt and is needed only for direct local inference.
# CITADEL's inference layer talks to Ollama via HTTP (see
# src/l3_adapters/ollama_adapter.py), so the dashboard, audit, regulatory,
# and federated layers all run with the slim deps below.

streamlit>=1.33.0
matplotlib>=3.7.0
pandas>=2.0.0
numpy>=1.24.0
httpx>=0.27.0
cryptography>=41.0.0
PyNaCl>=1.5.0
PyYAML>=6.0
